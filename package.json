{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "build":"tsc",
    "start":"node dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.7.3"
  }
}
