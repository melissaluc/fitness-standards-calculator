export enum Variation {
    Bodyweight = 'bodyweight',
    Weighted = 'weighted',
    Assisted = 'assisted'
}

export enum Gender {
    Male = 'male',
    Female = 'female'
}

export enum Service {
    Strength = 'strength',
    Row = 'row',
    Swim = 'swim',
    Run = 'run',
    Cycle = 'cycle'
}